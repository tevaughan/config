
" Either this entire block or else one or another Plugin line might be
" commented out. If the plugin be not installed, or if the operating system has
" its own installation of the plugin, then the Plugin line should be commented
" out. If no plugin should be enabled explicitly via vundle, then comment out
" the entire block.
"filetype off                       " . BEG COMMENTABLE SECTION.
"set rtp+=~/.vim/bundle/Vundle.vim  " .
"call vundle#begin()                " .
"Plugin 'VundleVim/Vundle.vim'      " . (Add other plugins after this.)
"Plugin 'Valloric/YouCompleteMe'    " .
"Plugin 'scrooloose/syntastic'      " .
"call vundle#end()                  " . END COMMENTABLE SECTION.

" Brief help
" :PluginList       - lists configured plugins
" :PluginInstall    - installs plugins; append '!' to update or just do
"                     :PluginUpdate
" :PluginSearch foo - searches for foo; append '!' to refresh local cache
" :PluginClean      - confirms removal of unused plugins; append '!' to
"                     auto-approve removal
"
" See ':h vundle' for more details or wiki for FAQ.

